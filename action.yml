# action.yml
name: 'Start Christotron'
description: 'Build and run Christotron'
# inputs:
#   who-to-greet:  # id of input
#     description: 'Who to greet'
#     required: true
#     default: 'World'
# outputs:
#   time: # id of output
#     description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'dockerfile'
  # args:
  #   - ${{ DOCKER_TOKEN }}